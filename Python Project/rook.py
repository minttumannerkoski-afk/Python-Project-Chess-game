from piece import Piece

class Rook (Piece):
    def __init__(self, position, color, status):
        super().__init__(position, color, status)

    def move(position, arrival):
        #if check(position, arrival):
            #why is it not with self
            position = arrival

    def check():
        return True